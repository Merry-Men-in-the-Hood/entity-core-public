"""
Intel Aggregator - STUB for public repository

Full implementation (source clients, normalization, severity scoring) is in
the private repository to prevent adversaries from evading detection.

Detection methods are kept private for the same reason antivirus
signature databases are kept private.
"""


def aggregate():
    """Aggregate threat signals from external intelligence sources."""
    raise NotImplementedError(
        "perception.intel_aggregator implementation is in the private repository."
    )
