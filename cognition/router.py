"""
LLM Router - STUB for public repository

Full implementation (task → brain mapping, threshold tuning) is in
the private repository.
"""
class Router:
    def route(self, task):
        raise NotImplementedError(
            "cognition.router implementation is in the private repository."
        )
